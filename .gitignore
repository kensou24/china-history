# 原始 EPUB（82MB 二进制，不入库）
*.epub

# 生成数据与图片（可由 scripts/ 重新生成）
# 注：web/public/ 下的同步副本已入库（部署用），此处只忽略管线源头 data/（锚定根目录）
/data/

# Node
node_modules/
dist/
*.local

# macOS
.DS_Store
__pycache__/
*.pyc

# 地图原始数据（Cliopatria / Natural Earth，手动下载，不入库）
/raw/
